# cargo-release configuration (https://github.com/crate-ci/cargo-release).
#
# This config tells cargo-release to bump every workspace crate to the
# same version in a single commit, but to NOT publish, tag or push. The
# rest of the release pipeline is owned by scripts/release.sh (which
# handles the CHANGELOG graduation and opens the PR) and by
# .github/workflows/release.yml (which creates the GitHub Release once
# the human pushes the tag). `nub` is not published to crates.io; see
# docs/RELEASE_PROCESS.md.

allow-branch = ["release/*"]
shared-version = true
consolidate-commits = true
publish = false
tag = false
push = false
sign-commit = false
sign-tag = false

# `{{version}}` is not rendered in the consolidated workspace commit context,
# so the message is kept static to avoid an unrendered placeholder landing in
# git history. The target version is recorded in CHANGELOG.md and the tag.
pre-release-commit-message = "release: bump workspace versions"
